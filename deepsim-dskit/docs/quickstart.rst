Quickstart
==========

Install in editable mode while developing:

.. code-block:: bash

   pip install -e ".[dev]"

Run a config from the command line:

.. code-block:: bash

   dskit-run --config configs/advertising_baseline.json --dry-run

Or run the offline example:

.. code-block:: bash

   python examples/advertising_pipeline.py
